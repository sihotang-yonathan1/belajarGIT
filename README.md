# belajarGIT

Daftar tugas / branch
1. Tugas-git
2. Tugas-html
3. Tugas-css
4. Tugas-js
5. Tugas-midProject
6. Tugas-php
7. Tugas-finalProject

Daftar perintah Git
```shell
git checkout -b Tugas-git
git add .
git commit -m "menambah Tugas-Git.txt"
git checkout main
git merge Tugas-git
git push

git checkout -b Tugas-html
git add .
git commit -m "menambahkan urutan ke-2: `Tugas-html.txt`"
git checkout main
git merge Tugas-html
git push

git checkout -b Tugas-css
git add .
git commit -m "menambahkan tugas ke-3: `Tugas-css.txt`"
git checkout main
git merge Tugas-css
git push

git checkout -b Tugas-js
git add .
git commit -m "Menambahkan tugas ke-4: `Tugas-js.txt`"
git checkout main
git merge Tugas-js
git push

git checkout -b Tugas-midProject
git add .
git commit -m "Menambahkan tugas ke-5: `Tugas-midProject`"
git checkout main
git merge Tugas-midProject
git push

git pull

git checkout -b Tugas-php
git add .
git commit -m "Menambahkan tugas ke-6: `Tugas-php.txt`"
git checkout main
git merge Tugas-php
git push

git log
git branch -a
git checkout -b Tugas-finalProject
git checkout main
git branch -d Tugas-finalProject
git status
git checkout -b Tugas-finalProject
git status
git add .
git commit -m "menambahkan tugas ke-7: `Tugas-finalProject`"
git status
git checkout main
git status
git log
git checkout Tugas-finalProject
git status
git log
git checkout main
git merge Tugas-finalProject
git push
```
